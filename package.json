{
  "name": "@prowi/deskcheck",
  "version": "0.2.0",
  "description": "Modular code review tool powered by Claude Agent SDK — define review criteria as markdown, run them against your codebase, get structured issues with code-level suggestions in a web dashboard.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Prowi-ApS/deskcheck.git"
  },
  "type": "module",
  "bin": {
    "deskcheck": "./build/cli.js",
    "deskcheck-mcp": "./build/mcp-server.js"
  },
  "files": [
    "build/",
    "ui/dist/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "npm run build && cd ui && npm ci && npx vite build"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.79",
    "@modelcontextprotocol/sdk": "^1.27.1",
    "commander": "^13.0.0",
    "gray-matter": "^4.0.3",
    "minimatch": "^10.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0",
    "vitest": "^3.0.0"
  }
}
